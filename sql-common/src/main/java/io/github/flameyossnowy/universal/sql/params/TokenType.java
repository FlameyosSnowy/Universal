package io.github.flameyossnowy.universal.sql.params;

enum TokenType {
        IDENT,
        KEYWORD,
        OPERATOR,
        QUESTION,
        LPAREN,
        RPAREN,
        COMMA,
        STRING
}